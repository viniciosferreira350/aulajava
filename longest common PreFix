class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        if (strs.length == 0) return "";
        String prefix = strrs[0]

        for (int i = 1; i <strs.length; i++ ) {
            while (strs[i].indexOF(prefix) !=0) {
                prefix = prefix.substring(0, prefix.length() -1);
            }
        }
        return prefix;
    }
}
